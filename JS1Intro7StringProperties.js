/* Properties
When you introduce a new piece of data into a JavaScript program, the browser saves it as an instance of the data type. Every string instance has a property called length that stores the number of characters in that string. You can retrieve property information by appending the string with a period and the property name:

Al ingresar un nuevo dato en un programa de JS, el navegador almacena este en la instancia de su tipo de dato. Cada cadena tiene una propiedad llamada longitud que almacena el numero de caracteres de esa cadena. Para recibir la informacion de la cadena ingresada y usar esta propiedad se usa:
*/

console.log('Hello'.length); // Prints 5

console.log('Teaching the world how to code'.length); // Prints 30